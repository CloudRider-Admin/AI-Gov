/**
 * Vitest global setup. Loaded before each test file.
 *
 * Currently a no-op stub — kept so vitest can locate `setupFiles` from
 * vitest.config.ts. Add global matchers, mocks, or environment shims here
 * as the test suite expands.
 */
